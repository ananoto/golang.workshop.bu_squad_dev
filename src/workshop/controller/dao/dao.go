package dao

import (
)
/*

func insert(bu model){
	//Database Connecting
	session := IntialDB()
	collection := session.DB("workshop").C("bu")
	defer session.Close()
	//Insert
	err := collection.Insert(bu)
	//Check null
	if err != nil {
		log.Fatal(err)
		return
	}
}*/
